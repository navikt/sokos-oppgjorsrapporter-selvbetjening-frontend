apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: sokos-oppgjorsrapporter-selvbetjening-frontend
  namespace: oppgjorsrapporter
  labels:
    team: oppgjorsrapporter
spec:
  observability:
    autoInstrumentation:
      enabled: true
      runtime: nodejs
  idporten:
    enabled: true
    sidecar:
      enabled: true
      level: Level3
  tokenx:
    enabled: true
  image: {{ image }}
  port: 3000
  liveness:
    path: /oppgjorsrapporter/api/internal/isAlive
    initialDelay: 10
  readiness:
    path: /oppgjorsrapporter/api/internal/isReady
    initialDelay: 10
  replicas:
    min: 2
    max: 4
  accessPolicy:
    outbound:
      rules:
        - application: nav-dekoratoren
          namespace: personbruker
        - application: sokos-oppgjorsrapporter
  ingresses:
    - "https://www.nav.no/oppgjorsrapporter"
  resources:
    limits:
      memory: 768Mi
    requests:
      cpu: "65m"
      memory: 672Mi
  env:
    - name: SOKOS_OPPGJORSRAPPORTER_API
      value: http://sokos-oppgjorsrapporter
    - name: SOKOS_OPPGJORSRAPPORTER_TOKEN_AUDIENCE
      value: "prod-gcp:oppgjorsrapporter:sokos-oppgjorsrapporter"
